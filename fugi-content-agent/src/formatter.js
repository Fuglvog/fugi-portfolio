// FORMATTER task: takes a raw caption + a platform key, returns the
// platform-shaped caption (char limits, hashtag style, newline rules).
// Stub — lands after COPY so we can shape real drafts.
throw new Error('formatter.js not implemented');
